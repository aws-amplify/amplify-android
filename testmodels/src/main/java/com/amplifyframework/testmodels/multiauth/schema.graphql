type Post
@model
@auth(rules: [
    { allow: owner },
    { allow: public operations:[read] }
]
)
{
    id: ID!
    name: String!
    description: String
    status: String @auth(rules:[{ allow: groups, operations:[update], groups: ["Approvers"] }])
}