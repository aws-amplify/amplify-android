type Parent @model {
  id: ID!
  name: String!
  address: Address!
  children: [Child]
}

type Child {
  name: String!
  address: Address!
}

type Address {
  street: String!
  street2: String!
  city: City!
  country: String!
}

enum City {
  FREETOWN
  BO
  MAKENI
  KENEMA
}
